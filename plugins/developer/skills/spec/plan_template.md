# Technical plan — <Feature Title>

**Spec:** [`.spec/<feature-slug>.md`](../.spec/<feature-slug>.md)
· **Branch:** `claude/feature/<feature-slug>`
· **RFC PR:** <pr-url, filled in once the draft PR is opened>
<!-- Optional rows — include only when real, never invented:
· **Ticket:** [<TICKET-ID>](<ticket-url>)
· **Primary repo:** [`<org>/<repo>`](<repo-url>), path root `<dir>/`   (only if more than one repo is touched)
· **Also touched:** [`<org>/<other-repo>`](<url>) (<what changes there>) -->

Grounded in the codebase at `<sha>` on <YYYY-MM-DD>.

<!-- Revisions, newest first, append-only:
> **Revision N (YYYY-MM-DD) — <summary>.**
> <body> -->

## Overview

What this plan covers, in a few sentences.

**Out of scope:** what this deliberately does *not* change, and why.

## Implementation Steps

1. ...

## Files / Components Affected

- ...

## Technical PRs

| # | PR | Scope | State |
|---|---|---|---|
| 1 | <org>/<repo>#<n> | ... | open / merged (`<sha>`) / dropped |

## Verification

1. `<command>` → expected: ...  (covers step N)

## Open Questions

<!-- Implementation-level only. Numbered; never renumber. Closed: ~~**O-1** — …~~ ✅ <answer> -->
- **O-1** — <question>.

## Decided

- **<decision>** — <why>.
- ~~**<superseded decision>**~~ **Corrected <YYYY-MM-DD>:** <what changed and why>.

## Risks

- ...
